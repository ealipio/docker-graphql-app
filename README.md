# Docker GraphQL simple app

using graphql with apollo server in a docker container

building and pushing :
```bash
$ docker build -t eisson/docker-graphql-app:latest .
$ docker push  eisson/docker-graphql-app:latest 
```